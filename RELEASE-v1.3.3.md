# AMD-DLSS-MU v1.3.3

## 更新内容

- 发布版内置 `nvngx_dlssnr.dll`，用户无需另行下载或选择该文件。
- 构建缺少该 DLL 时直接失败，避免生成缺少模式一组件的发布包。
- 模式一、模式二及启动器更新下载失败时，自动尝试可用的 GitHub 官方 API 附件入口，无需配置下载源。
- 模式一发布信息查询失败时，使用内置的已核验版本信息继续尝试下载。
- 增加下载进度、超时处理、文件大小及 SHA-256 校验，并保留下载失败诊断信息。

## 使用说明

下载附件 `AMD-DLSS-MU.exe`；已有版本也可通过“检查更新”升级。

模式一仍使用 DLSS-NR-on-AMD v0.3.1，要求 Windows 11；安装后按上游说明在游戏内开启 FSR 3 / FSR 4，通过 End 打开原生面板。

备用入口仍属于 GitHub，并非独立镜像，不能保证所有网络均可直连。上游安装器等依赖仍可能需要联网下载。

## 验证

- 54 项自动化测试通过。
- 发布 EXE 已核验包含内置 DLL。
- 本次未进行 Windows 真机游戏兼容性测试，不承诺所有游戏或显卡均可正常运行。

文件：`AMD-DLSS-MU.exe`（331230561 字节）

SHA-256：`4e8f18921889d4c8e584715519c910b4b07a66e84f62b18441ea9b614671e90b`
