# 恢复配置（v1.2.1）

关闭游戏，选择实际游戏 EXE，点击“恢复配置”。有安装记录时，确认清单后还原安装前备份并移除本次新增组件；已改动的受管理 INI 会先另存备份。二进制被外部修改、备份损坏、游戏仍运行或安装记录处于意外中断状态时停止操作并提示原因。

没有记录或记录已恢复时，可以查看旧组件候选清单。候选文件名不代表文件一定属于本工具：其中的 winmm.dll、dxgi.dll 等也可能属于其他插件。确认后移到 `%LOCALAPPDATA%/AMD-NR-Assistant/games/<摘要>/legacy-<编号>`，保留文件和 manifest.json。界面显示完整备份位置。如需撤销，关闭游戏，将备份中所需文件复制回清单指定的原游戏目录；不要覆盖后来安装的文件。

缺少安装前备份的旧安装不能保证恢复到最初状态。清理仅覆盖界面列出的平铺文件，不扫描子目录，也不删除游戏自带的 nvngx_dlss.dll、nvngx_dlssg.dll 或 d3d12.dll。未知组件不在清单中时使用对应安装器卸载。

恢复按钮不再依赖安装记录才可用。安装阻挡原因会显示在弹窗中；诊断导出新增 InstallationBlocked 和 ConflictingFiles，防止忽略冲突的检查结果误导用户。

已通过 21 项管理测试及 Windows x64 单文件发布构建。尚需 Windows 实机验证 UI、游戏占用和不同磁盘备份行为。
