# AMD DLSS MU v1.3.2

- 模式一运行时从 DLSS-NR-on-AMD v0.3.0 升级至 v0.3.1。上游修复停顿、帧生成冻结画面、RE Engine 崩溃及内存泄漏；具体效果需游戏实测。
- 按固定版本查询上游，线上下载和本机缓存都验证固定大小与 SHA-256，避免上游更新导致已核验版本无法安装。
- 增加内置 DLL 哈希校验及游戏目录写入、安装器退出后的 DLL 诊断记录。
- DLL 要求仍为 nvngx_dlssnr.dll 310.8.0.0。

已有模式一安装不会自动升级游戏目录组件。请关闭游戏，在启动器中恢复配置后重新安装模式一。
上游安装器仅在运行时下载，不打包在启动器内。

编译及 47 项自动测试通过；已确认发布 EXE 包含完整内置 DLL，游戏内效果仍需实测。

AMD-DLSS-MU.exe SHA-256：
`6df2f35d86933dcbbf6af9920895373b738d85382161b8b6cb0fc991b561fdff`
