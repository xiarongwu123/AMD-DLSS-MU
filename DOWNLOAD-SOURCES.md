# 自动下载

用户直接点击“一键配置”或“检查更新”，无需选择下载源、填写地址或编辑配置文件。

下载失败后自动尝试官方 API 附件入口；每次下载均核验文件大小与 SHA-256。连接等待 15 秒、读取无进展 45 秒，每个入口最多 30 分钟。取消后停止，切换入口时重新下载。

模式一查询发布信息失败时，按内置的固定版本、大小和哈希继续尝试下载。模式二固定运行时及附件 ID。启动器更新仍需先取得官方发布信息。

当前备用入口仍属于 GitHub，未部署独立镜像，不能保证所有直连网络都可用。后续由维护者统一接入可用源，不向用户暴露配置。
