# 游戏管理与诊断

游戏页增加「兼容性 / 记录」「刷新运行状态」「导出诊断」。

- 安装前检查 Windows 11、x64 EXE、显卡与 Windows 驱动信息、同目录反作弊标识和代理冲突。图形接口/FSR 文件证据不是实际运行证明，未经实测的组合标为未验证；不将 Windows 驱动版本当作 Adrenalin 市场版本。
- 每游戏记录保存在 `%LOCALAPPDATA%/AMD-NR-Assistant/games/<路径摘要>/record.json`，记录模式、组件版本、时间、阶段及文件哈希，备份在同一目录。重启后自动读取。
- 新安装会记录两个后端涉及的平铺组件。模式二仅接受当前支持的平铺包；未审查的子目录包拒绝安装。此版本未实现模式二全自动资源下载。
- 切换模式先恢复再安装。未知代理冲突会阻止覆盖。旧版安装没有完整文件所有权记录，必须先使用原安装器卸载；不会从文件名猜测所有权。
- 恢复前检查所有文件和备份，外部修改或损坏会阻止操作。新增组件被移除，原组件恢复，备份保留。安装器意外中断且未记录最终状态时需人工恢复；不会把不明文件当成本程序创建。
- 状态观察以当前游戏进程的启动时间和首次观察的日志位置为界，只读取后续日志。`dlss5-neural` 的 `frame N processed (M skipped)` 可报告处理证据。其他运行时未适配的日志仍显示未验证，不把加载、开关或耗时当成功推理。
- 诊断 JSON 在本地预览后保存，包含组件哈希、完整性比较和固定错误类别，不包含原始日志、游戏路径、用户名或序列号，不自动上传。

## 验证

`dotnet run --project tests/Management.Tests.csproj`

自动化覆盖持久化、每游戏隔离、外部修改、恢复预检查、备份损坏、重复恢复、意外中断、清单路径穿越和日志匹配。需要 Windows AMD 硬件继续验证 UI、模块枚举、驱动识别、上游安装器产物和游戏内效果。目录扫描不能证明所有反作弊或静态链接 API 都被发现。
