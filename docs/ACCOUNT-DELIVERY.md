# MU 账号系统交付记录

日期：2026-09-26。开发分支：`codex/account-membership`。

## 已实现

- .NET 8 WinForms 客户端接入邮箱验证码注册、密码登录、找回/修改密码、退出、DPAPI 保持登录和账户中心。
- 启动必须验证登录；每 30 秒检查会话，业务操作实时授权。按钮、键盘、拖放、下载重试/续传及独立面板均接入权限检查。
- 普通/Pro、会员期限及固定功能权限由服务端决定。失联禁止新操作；已经进入游戏文件写入的事务完成安全收尾。
- .NET 10 API、Identity、独立 SQLite 数据库及 Razor 管理后台同仓维护。管理员独立 Cookie、TOTP、CSRF、实时身份撤销及操作审计。
- 后台管理用户、会话、人工 Pro 授权、套餐、订单及功能权限。功能配置有独立递增版本，规则与前后版本审计同事务保存。支付适配器只包含禁用实现，无虚假付款成功路径。
- 注册、验证码发送、密码重置均使用 Resend 适配器，未配置时明确失败；日/月额度耗尽、限流和一般发送失败分别提示。生产注册与支付默认关闭。

## 已验证

| 验证 | 结果 |
| --- | --- |
| 服务端 SQLite 业务及迁移测试 | 61 项断言通过 |
| API HTTP 与邮件错误映射集成测试 | 64 项断言通过 |
| 后台表单、双因素及备份集成 | 92 项断言通过 |
| 客户端账号 HTTP 回归 | 30 项断言通过 |
| 游戏管理、下载、Magpie 回归 | 77 项断言通过 |
| 更新版本校验 | 15 项断言通过 |
| UI 编译、Windows 发布、Linux 服务发布 | 成功；UI 编译零警告零错误 |
| VPS 回环健康、HTTPS 代理头、未登录拒绝、关闭注册与支付 | 通过 |
| 容器重启持久化、SQLite 备份、隔离恢复后重新加载 | 通过 |
| v1 → v2 升级、迁移失败事务回滚 | 测试通过；VPS 实际 v1 备份副本升级并保持全部规则 |
| 现有官网下载服务健康检查 | 仍为 200；统计库已另做一致性备份 |
| 公网 HTTPS 与本机 Chrome 后台入口 | 健康接口 200；根地址跳转登录页，Chrome 已实际显示管理员登录表单 |

账号测试使用隔离数据库和测试邮件发送器，不能代替真实收信验收。Mac 编译与打包不能代替 Windows 实机验收。

## 当前部署

- VPS：`72.11.138.132`，SSH 端口 `1013`，用户 `xrw`。
- 目录：`/home/xrw/amd-dlss-mu-account`，当前服务版本目录：`releases/20260926-03`；数据库 schema 2。升级前已使用 v1 运行容器备份，随后从真实备份副本验证升级，再激活正式服务。域名根路径已增加到 `/admin` 的跳转，并在 VPS 验证通过。
- 容器：`amd-dlss-mu-account`，镜像 `mcr.microsoft.com/dotnet/aspnet:10.0.12`，只绑定 `127.0.0.1:8089`。
- 数据库与密钥持久化在 `data/`，服务密钥只在服务器 `.env`，权限 `0600`。
- 每日 `03:00 UTC`（北京时间 `11:00`）一致性备份，保留最近七份成功快照。部署与备份共用锁；备份通过完整性校验后才原子发布。
- `mu-api.claude-api.cn` 的 Cloudflare Tunnel 路由与 DNS 已发布到 `127.0.0.1:8089`；公网健康接口与后台登录页返回 200，根地址跳转 `/admin`，本机 Chrome 已显示管理员登录表单。
- 本机原有 Clash `DOMAIN-KEYWORD,claude` 规则将该域名送入代理并导致连接关闭。已在当前订阅的持久规则覆写中前置 `DOMAIN,mu-api.claude-api.cn,DIRECT`，重新激活订阅后验证正常；配置对比确认只有这条规则新增，修改前文件已备份。无需关闭全局代理或 TUN。
- Resend 已验证 `mu-mail.claude-api.cn`，服务器发送密钥已配置且只保存在权限为 `0600` 的 `.env`。测试邮件及应用实际注册验证码邮件均已投递，公开注册已开启。
- 首个管理员已通过服务器 CLI 创建，随机高强度密码只保存在本机 macOS 钥匙串的 `MU 管理后台` 项；管理员首次登录后的身份验证器绑定仍待人工完成。

部署、管理员初始化和恢复操作见 [服务说明](../server/README.md)；数据库版本升级规则见 [数据库说明](../server/Mu.Server/DATABASE.md)。

## 客户端预览包

- 文件：`artifacts/v2.0.0-preview.2-account-win-x64/AMD-DLSS-MU.exe`。
- 版本：`2.0.0-preview.2`，文件版本：`2.0.0.1`。
- SHA-256：`ecb188eac55f8a4db3b0d8b76757a32e0609e32cf8e19d96dec16ad3744271e1`。
- 尚未替换官网下载；现有客户端及已安装游戏组件保持原有行为。

## 上线剩余验收

1. 管理员首次登录并绑定身份验证器，确认后台用户、权限、会员和审计页面。
2. Windows 实机完成注册、登录、重启保持登录、重置密码、退出、单实例、DPI 和键盘入口验证。
3. Windows 验证后台禁用/权限变化/Pro 到期，以及下载中和安装/恢复文件写入期间断网或撤销会话的行为。
4. 账号闭环验收后再发布新客户端。商户、价格和权益确定后，另行接入并验收真实支付。
