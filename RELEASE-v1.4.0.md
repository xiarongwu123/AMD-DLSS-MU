# AMD DLSS MU v1.4.0

## 桌面界面更新

- 原生 WinForms 两栏布局：左侧导航、右侧游戏库；使用官网深色与荧光绿配色。
- 新增可持久化的日间 / 夜间皮肤，可从侧栏或设置页随时切换。
- 修复 125% / 150% / 200% DPI 下页面标题被裁切、侧栏文字省略和间距失真的问题。
- 游戏库底部突出「一键配置」，已配置游戏可直接启动。
- 配置完成弹窗提供「返回游戏库」与「启动游戏」。
- 安装方案及 OptiScaler 图形接口收进「高级选项」。
- 游戏封面按原始比例布局，不裁切或拉伸；缺少封面时保留游戏图标。
- 新增下载任务页，显示真实进度、下载源与 SHA-256 校验结果，支持暂停、继续、取消、失败重试。
- 启动器更新下载也接入任务页，保留更新前校验与回滚机制。
- 新增帮助、设置页面；关于页保留作者微信二维码、复制微信号、放大二维码和第三方许可入口。
- 默认进入游戏库；保留手动添加、扫描、恢复配置、运行诊断及 F8 面板。

## 边界

- 模式一仍使用上游官方交互式安装器，授权及安装器提示需要用户确认；不自动跳过安全检查。
- 模式二默认为 DirectX 11/12；Vulkan 游戏需在高级选项选择对应接口。
- 下载记录为本次应用会话；暂停不代表跨重启断点续传。失败重试会重新请求并完整校验文件。
- 启动请求不等于游戏已成功运行；组件安装完成不等于渲染效果已经生效。
- WinForms 原生窗口标题栏、系统文件选择器和安全提示保留 Windows 行为。

## 验收清单

- Windows 11，100% / 125% / 150% / 200% DPI：游戏库、下载、关于、设置页面无重叠；窗口缩放后封面比例不变。
- 没有游戏 / 搜索无结果 / 没有封面 / 长游戏名。
- 模式一和模式二安装；重复安装保护；恢复后状态刷新；配置完成后启动。
- Steam 游戏通过 Steam 启动，手动添加游戏从 EXE 所在目录启动。
- 下载暂停、继续、取消、源切换、错误校验值、断网重试。
- 手动检查更新、更新取消、下载校验、应用重启与健康确认。
- 微信二维码缩放与复制；诊断导出；已有安装记录兼容。

此环境为 macOS，交叉编译和跨平台核心测试不能替代上述 Windows 界面及游戏实机验收。
